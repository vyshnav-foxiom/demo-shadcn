import InvoiceTable from '@/components/Web/InvoiceTable'
import authData from '../../utils/LogData.json'
import DataTableDemo from '@/components/demo/Table'
import {
  AlertDialog,
  AlertDialogAction,
  AlertDialogCancel,
  AlertDialogContent,
  AlertDialogDescription,
  AlertDialogFooter,
  AlertDialogHeader,
  AlertDialogTitle,
  AlertDialogTrigger,
} from "@/components/ui/alert-dialog"

const Admin = () => {
  // (authData.map((data) => (
  //   console.log(data.username, data.password)
  // )))
  return (

    <>
      <h1>Admin</h1>

    </>


  )
}
export default Admin